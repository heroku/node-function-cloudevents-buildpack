{
  "name": "node-function-cloudevents-buildpack",
  "version": "0.0.1",
  "description": "",
  "main": "cloudevents.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cloudevents-sdk": "^0.3.0"
  }
}
